<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TodayInHistory" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#D4A017</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#333333</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- 이미지 둥근 모서리 스타일 -->
    <style name="RoundedImageView" parent="">
        <item name="cornerSize">12dp</item>
    </style>

    <!-- 잠금화면용 풀스크린 테마 -->
    <style name="Theme.TodayInHistory.LockScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>